# {projectName}

A simple website built with HTML, CSS, and JavaScript.

## Project Structure
```
{projectName}/
├── index.html      # Main page
├── style.css       # Styles
├── script.js       # JavaScript
├── CLAUDE.md       # This file
└── .gitignore
```

## Development Guide
- Open `index.html` in your browser to preview
- Edit `style.css` for styling changes
- Add interactivity in `script.js`
- Keep it simple — start with what works, improve later

## Coding Standards
- Use semantic HTML elements (header, main, section, footer)
- Mobile-first responsive design
- Prefer vanilla JS over frameworks for simple sites
- Use CSS custom properties for theming

## Security Rules
- NEVER read or output contents of .env or credential files
- NEVER hardcode API keys, tokens, or passwords
- Always use environment variables for sensitive configuration
- Validate all user input before processing
